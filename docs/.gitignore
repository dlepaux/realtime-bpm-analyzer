# VitePress build output
.vitepress/dist
.vitepress/cache
.vitepress/.temp

# TypeDoc legacy HTML output (we use markdown now)
docs/
api/

# Dependencies
node_modules/

# System files
.DS_Store

# Copied from root
contributing.md
code-of-conduct.md
security.md
privacy.md

# Generated assets
# Note: public/favicon/ is committed (pre-generated) so the docs build does not
# depend on the `realfavicon` toolchain, which transitively pulls an unfixable
# image-size advisory. Regenerate with `npm run build:favicon` if the brand icon changes.
favicon.json
